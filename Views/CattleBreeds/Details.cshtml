@model Jallikattu.Models.CattleBreedsTable

@{
    ViewBag.Title = "Cattle Breed Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Cattle Breed Details</h2>

        <div>
            <a href="@Url.Action("Edit", new { id = Model.CattleID })"
               class="btn btn-primary">
                Edit
            </a>

            <a href="@Url.Action("Delete", new { id = Model.CattleID })"
               class="btn btn-danger">
                Delete
            </a>
        </div>
    </div>

    <!-- Main Card -->
    <div class="card shadow-sm mb-4">
        <div class="card-body">

            <div class="row">
                <!-- Image -->
                <div class="col-md-4 text-center">
                    <img src="@Model.ImageURL"
                         alt="@Model.BreedName"
                         class="img-fluid rounded shadow-sm"
                         style="max-height:250px;">
                </div>

                <!-- Basic Info -->
                <div class="col-md-8">
                    <h3 class="fw-bold">@Model.BreedName</h3>

                    <p><strong>Utility:</strong> @Model.Utility</p>
                    <p><strong>Origin:</strong> @Model.Origin</p>
                    <p><strong>Distribution:</strong> @Model.Distribution</p>
                </div>
            </div>

            <hr />

            <!-- Description -->
            <div class="mt-3">
                <h5>Description</h5>
                <p>@Model.Description</p>
            </div>

        </div>
    </div>

    <!-- Admin Metadata -->
    <div class="card shadow-sm">
        <div class="card-header bg-light fw-bold">
            Admin Information
        </div>

        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <p><strong>Created By:</strong> @Model.Createdby</p>
                    <p>
                        <strong>Created At:</strong>
                        @Model.CreatedAt.ToString("dd MMM yyyy, hh:mm tt")
                    </p>
                </div>

                <div class="col-md-6">
                    <p><strong>Updated By:</strong> @Model.UpdatedBy</p>
                    <p>
                        <strong>Updated At:</strong>
                        @Model.UpdatedAt.ToString("dd MMM yyyy, hh:mm tt")
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Back Button -->
    <div class="mt-3">
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
            ← Back to List
        </a>
    </div>

</div>
